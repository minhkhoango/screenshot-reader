{
  "name": "screenshot-reader",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && rm -rf dist && VITE_ENTRY=background vite build && VITE_ENTRY=content vite build && cp public/manifest.json dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "@types/node": "^25.0.3",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
